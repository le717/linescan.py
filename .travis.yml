language: python
python:
   - "3.3"
   - "3.2"
   - "2.7"
   - "2.6"
   - "pypy"

# Install flake8
install:
  - pip install -r requirements.txt --use-mirrors
before_script:
  - python tests.py write
script:
  - flake8 --max-complexity 10 linescan.py
  - python tests.py
