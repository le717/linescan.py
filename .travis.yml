language: python
python:
  - "3.3"
  - "3.2"
install:
  # Build/test dependencies
  - pip install -r requirements.txt --use-mirrors
before_script:
  - python testusage.py write
script:
  - python testusage.py scanme
