language: python
python:
   - "3.3"
   - "3.2"
   - "2.7"
   - "2.6"
   - "pypy"
install:
  # Build/test dependencies
  - pip install -r requirements.txt --use-mirrors
before_script:
  - python tests.py write
script:
  - python tests.py
